import 'package:flutter_test/flutter_test.dart';

// Smoke tests live in test/features/. This file is intentionally minimal.
void main() {
  test('placeholder — see test/features/ for feature tests', () {
    expect(1 + 1, equals(2));
  });
}
